[
  {
    "mutation": ">= → >",
    "snippet": "return { valid: forge > 0 && resolve >= KAPPA * forge, forge, resolve };",
    "reason": "equivalent: KAPPA = 1/φ is irrational, so resolve == KAPPA·forge is impossible for integer resolve/forge — `>=` and `>` never differ"
  },
  {
    "mutation": "> → >=",
    "snippet": "while (this.recent.some((r) => { const d = wrap(theta - r); return d < 0.06 || d > TAU - 0",
    "reason": "equivalent: d is a continuous wrapped angle; d == TAU-0.06 exactly is measure-zero (never hit), so `>` and `>=` never differ on any reached input"
  },
  {
    "mutation": "< → <=",
    "snippet": "while (this.recent.some((r) => { const d = wrap(theta - r); return d < 0.06 || d > TAU - 0",
    "reason": "equivalent: the two `<` here are a measure-zero float boundary (d == 0.06, never hit) and the stall-guard iteration cap (g reaches 1 when the guard fires, never near 6) — neither differs on any reached input"
  },
  {
    "mutation": ">= → >",
    "snippet": "const c = Number.isFinite(cycle) && cycle >= 0 ? Math.floor(cycle) : 0;",
    "reason": "boundary identity: >= and > differ only at cycle === 0, and both arms then yield the same 0 (Math.floor(0) on one side, the sanitised default on the other). No input separates them."
  },
  {
    "mutation": ">= → >",
    "snippet": "const n = (v) => (Number.isFinite(v) && v >= 0) ? v : 0;",
    "reason": "boundary identity: >= and > differ only at v === 0, and both arms then yield the same 0 (the value itself on one side, the sanitised default on the other). No input separates them."
  },
  {
    "mutation": "< → <=",
    "snippet": "return { ok: false, why: idx < p.stage",
    "reason": "guard-shadowed identity: this ternary runs only inside the idx !== p.stage branch, so idx === p.stage is unreachable and < and <= cannot be separated by any input. The comparison only chooses which refusal sentence is spoken."
  },
  {
    "mutation": "> → >=",
    "snippet": "const n = Number.isInteger(attempt) && attempt > 0 ? attempt : 0;",
    "reason": "boundary identity: > and >= differ only at attempt === 0, and both arms then yield 0 (the value itself on one side, the sanitised default on the other). No input separates them."
  },
  {
    "mutation": "< → <=",
    "snippet": "if (at && (entry.firstAt === null || at < entry.firstAt)) entry.firstAt = at;",
    "reason": "Genuinely equivalent: on a tied earliest timestamp, entry.firstAt is reassigned to the IDENTICAL string it already holds — a self-assignment with no observable effect on any output. This differs from the lastAt line (kept strict, not baselined), where a tie still updates `sample` as a real side effect, so that boundary IS observable and IS tested. No test can distinguish < from <= here because both produce the same result on every input; restructuring the comparison cannot remove the equivalence, only relocate it."
  }
]
